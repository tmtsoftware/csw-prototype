package csw.util.cfg;

import org.junit.Test;
import csw.util.cfg.Events.ObserveEvent;
import csw.util.cfg.Events.EventTime;

import java.util.Date;

/**
 * Tests using the config based classes from Java
 */
public class ConfigJavaTests {
    @Test
    public void testEvent() throws Exception {
//        String prefix = "";
//        EventTime t = new EventTime(new Date().getTime());
//        ObserveEvent oe = ObserveEvent.make(prefix, t);
        // TODO...
//                .set(eventNum, num)
//                .set(exposureTime, 1.0)
//                .set(imageData, testImageData)

    }
}

