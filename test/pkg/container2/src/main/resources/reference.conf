// Akka config file

akka {
    log-dead-letters = 0
    loglevel = "INFO"

    actor {
        debug {
          // autoreceive = off
        }
    }
    remote {
        // log-remote-lifecycle-events = off
    }
}
